# Python
__pycache__/
*.pyc

# Logs & runtime artifacts
*.log
data/*.log
data/refresh.err.log
data/.discovery-*/
data/*.sqlite3-journal
data/*.sqlite3-wal
data/*.sqlite3-shm

# Large raw research dumps (not needed by the app)
# (award source tables raw_pew.txt / raw_nih_dp2.txt / raw_searle.txt / raw_klingenstein.txt
#  ARE needed by build_awards.py on CI, so they are committed explicitly below.)
data/raw_*.txt
!data/raw_pew.txt
!data/raw_nih_dp2.txt
!data/raw_searle.txt
!data/raw_klingenstein.txt

# OS noise
.DS_Store

# Local launchd (use GitHub Actions instead when hosted)
# com.neuro.cns.refresh.plist  # keep in repo as reference
